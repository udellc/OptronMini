/*name:		nw_reroute.jsfunction:	does the opposite of route; a kind of multi-prependcreated:	2004.07.20 --- Nathan Wolek© 2004, lowkey digital studio*///globalif (jsarguments.length>1){	inlets = (jsarguments.length - 1);	var p = new Array(inlets);	for(i=0; i<p.length; i++)	{		p[i] = jsarguments[i+1];	}}else{	inlets = 1;	var p = 0;}oulets = 1;setinletassist(-1,in_describe);setoutletassist(-1,out_describe);function anything(){	var outmess = arrayfromargs(messagename,arguments);	outmess.unshift(p[inlet]);	outlet(0,outmess);}function in_describe(num){	assist("prepend input with " + p[num]);}function out_describe(num){	assist("prepended inputs");}